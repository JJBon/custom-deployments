apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: redis
  namespace: langgraph
spec:
  interval: 5m
  chart:
    spec:
      chart: redis
      version: ">=19.0.0"
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: langgraph
  values:
    auth:
      enabled: true
      password: lgredispw
    architecture: standalone
    master:
      persistence:
        enabled: false   # set true + size if you want persistence
